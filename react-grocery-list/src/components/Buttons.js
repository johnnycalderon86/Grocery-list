import React from 'react'

export default function Buttons() {
    return (
        <div>
        <button style={styleButtons}>List</button>
        <button style={styleButtons}>Stats</button>
        <button style={styleButtons}>Recipes</button>
        </div>
    )
}
